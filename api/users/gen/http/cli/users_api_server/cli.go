// Code generated by goa v3.2.6, DO NOT EDIT.
//
// usersAPIServer HTTP client CLI support package
//
// Command:
// $ goa gen github.com/enkhalifapro/users-serverless/internal/users/api/design
// -o ./api/users

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	usersapic "github.com/enkhalifapro/users-serverless/api/users/gen/http/users_api/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `users-api (add|get)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` users-api add --body '{
      "password": "Cumque aspernatur.",
      "username": "Et asperiores non consequatur occaecati."
   }'` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		usersAPIFlags = flag.NewFlagSet("users-api", flag.ContinueOnError)

		usersAPIAddFlags    = flag.NewFlagSet("add", flag.ExitOnError)
		usersAPIAddBodyFlag = usersAPIAddFlags.String("body", "REQUIRED", "")

		usersAPIGetFlags  = flag.NewFlagSet("get", flag.ExitOnError)
		usersAPIGetIDFlag = usersAPIGetFlags.String("id", "REQUIRED", "user id")
	)
	usersAPIFlags.Usage = usersAPIUsage
	usersAPIAddFlags.Usage = usersAPIAddUsage
	usersAPIGetFlags.Usage = usersAPIGetUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "users-api":
			svcf = usersAPIFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "users-api":
			switch epn {
			case "add":
				epf = usersAPIAddFlags

			case "get":
				epf = usersAPIGetFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "users-api":
			c := usersapic.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "add":
				endpoint = c.Add()
				data, err = usersapic.BuildAddPayload(*usersAPIAddBodyFlag)
			case "get":
				endpoint = c.Get()
				data, err = usersapic.BuildGetPayload(*usersAPIGetIDFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// users-apiUsage displays the usage of the users-api command and its
// subcommands.
func usersAPIUsage() {
	fmt.Fprintf(os.Stderr, `Users API
Usage:
    %s [globalflags] users-api COMMAND [flags]

COMMAND:
    add: Add a new user.
    get: Get user by id.

Additional help:
    %s users-api COMMAND --help
`, os.Args[0], os.Args[0])
}
func usersAPIAddUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] users-api add -body JSON

Add a new user.
    -body JSON: 

Example:
    `+os.Args[0]+` users-api add --body '{
      "password": "Cumque aspernatur.",
      "username": "Et asperiores non consequatur occaecati."
   }'
`, os.Args[0])
}

func usersAPIGetUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] users-api get -id STRING

Get user by id.
    -id STRING: user id

Example:
    `+os.Args[0]+` users-api get --id "Voluptates eaque est."
`, os.Args[0])
}
